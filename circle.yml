
machine:
  node:
    version: 0.10.33

dependencies:
  pre:
    - npm i -g normajs
  override:
    - sudo pip install awscli

test:
  pre:
    - norma build
  override:
    - norma test


deployment:
  aws:
    branch: master
    commands:
      - aws s3 cp ./junction.js s3://cdn.newspring.cc/
