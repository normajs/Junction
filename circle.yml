
machine:
  node:
    version: 0.10.33

dependencies:
  pre:
    - npm i -g normajs

test:
  pre:
    - norma build
  override:
    - norma test


# deployment:
#   production:
#     branch: master
#     commands:
#       - npm i ci-npm-publish -g
#       - npm-publish
